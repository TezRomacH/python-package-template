# Poetry pyproject.toml: https://python-poetry.org/docs/pyproject/

[build-system]
requires = ["poetry>=1.0"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "{{ cookiecutter.project_name }}"
version = "{{ cookiecutter.version }}"
description = "{{ cookiecutter.project_description }}"
readme = "README.md"
authors = []  # Update me
license = "{{ cookiecutter.license }}"
repository = "https://github.com/{{ cookiecutter.git_name }}/{{ cookiecutter.project_name }}"
homepage = "https://github.com/{{ cookiecutter.git_name }}/{{ cookiecutter.project_name }}"

keywords = []  # Update me

# Pypi classifiers: https://pypi.org/classifiers/
classifiers = [  # Update me
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  "Topic :: Software Development :: Libraries :: Python Modules",
]

[tool.poetry.dependencies]
python = "^3.7"
typer = {extras = ["all"], version = "^0.2.1"}

[tool.poetry.dev-dependencies]
darglint = "^1.2.2"
isort = "^4.3.21"
pyupgrade = "^2.3.0"
black = "^19.10b0"
mypy = "^0.770"
bandit = "^1.6.2"
safety = "^1.9.0"

[tool.poetry.scripts]
"{{ cookiecutter.project_name }}" = "{{ cookiecutter.project_name.lower().replace('-', '_') }}.__main__:app"

[tool.black]
# https://github.com/psf/black
line-length = 80
target-version = ["py37"]

[tool.isort]
# https://github.com/timothycrosley/isort/
known_typing = "typing,types,typing_extensions,mypy,mypy_extensions"
known_ds = "pandas,numpy,torch,torchvision,pytorch_lightning,catalyst"
sections = "FUTURE,TYPING,STDLIB,FIRSTPARTY,DS,THIRDPARTY,LOCALFOLDER"
include_trailing_comma = true
default_section = "FIRSTPARTY"
multi_line_output = 3
force_grid_wrap = 0
use_parentheses = true
line_length = 80
